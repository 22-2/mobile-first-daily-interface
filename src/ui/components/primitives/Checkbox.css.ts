import { style } from "@vanilla-extract/css";

export const label = style({
  display: "inline-flex",
  alignItems: "center",
  gap: "0.5rem",
  cursor: "pointer",
});

export const input = style({});
